"""
JSON Schema is a package that contains the code to parse and read the Ontology Schema JSON file.

The JSON file is read into memory and then used to build an in-memory data structure.

**Note** This package uses pydantic.
"""
