"""
The transfer_table package contains classes to wrap the Ontology database.

This is only used to validate the tables in the Ontology database and to facilitate
the transfer of database information from the old schema to the new schema.
"""
